# app

Application shell, providers, routing, navigation, and platform-level layout live here.

The default `/settings` route tree is disposable example scaffolding. Replace or remove it once the generated app has real product settings and auth requirements.
