# app-dev Audit Closeout Ledger

Source audit: `C:\Users\piete\Downloads\app-dev-audit.md`

Status values:

- `closed`: already closed in the live checkout before this closeout pass.
- `open-fixed-by-this-plan`: implemented or validated by this closeout pass.
- `accepted-decision`: intentionally not implemented because the safer project policy is to defer.

## P0 Findings

| Finding | Status | Closure |
| --- | --- | --- |
| Decide whether `.codex/config.toml` is documentary or active | closed | `.codex/config.toml` is active with project permissions, inline hooks, and validation coverage. |
| Remove empty `projects/__verify-expo` and `projects/__verify-next` directories | closed | No disposable verification folders are present; `.gitignore` ignores generated `projects/*` while preserving `projects/.gitkeep`. |
| Add local validation for TOML, package JSON, skill frontmatter, name/path match, AGENTS size, and references | closed | `scripts/validate-codex-assets.ps1` performs these checks and is wired into CI. |
| Add `.codex/rules/default.rules` | closed | Rules file exists with forbidden and prompt decisions for destructive, deployment, and database commands. |
| Document required local tools | closed | `README.md` documents Git, PowerShell 7, Node.js LTS, Node 22 recommendation, Python 3.11+, Codex, Playwright, Android Studio, Xcode, and Supabase CLI conditions. |

## P1 Findings

| Finding | Status | Closure |
| --- | --- | --- |
| Shorten and front-load the skill description | open-fixed-by-this-plan | `cross-platform-app-workflow` description now starts with app-dev scaffold/verify/review trigger terms. |
| Mark external skills/plugins as optional global dependencies | closed | `standards/codex-capabilities.md`, `.agents/README.md`, `.agents/skills/README.md`, and the skill all mark external capabilities as optional. |
| Add generated app `AGENTS.md` placeholders with exact verification and done-when criteria | open-fixed-by-this-plan | All app templates now include product decision record, exact `verify-app.ps1` command, and `Done When` criteria. |
| Add `PLANS.md` or planning template | closed | `PLANS.md` and `templates/PLAN.template.md` exist and are generated by `scripts/create-app.ps1`. |
| Add a project creation checklist that forces product decision record completion before feature work | open-fixed-by-this-plan | Template instructions, generated plans, validation, and `create-app.ps1` enforce the checklist text. |

## P2 Findings

| Finding | Status | Closure |
| --- | --- | --- |
| Update JS dependencies to current compatible majors before production app work | open-fixed-by-this-plan | Template package baselines are refreshed to current compatible major ranges for the documented local Node LTS floor. |
| Add minimal runnable source to Next and Expo templates | open-fixed-by-this-plan | Next and Expo templates now include minimal app source, config, and smoke tests. |
| Add Tailwind/shadcn initialization path to React template | open-fixed-by-this-plan | React template includes Tailwind/shadcn config files and README instructions. |
| Add CI workflow template for generated apps | open-fixed-by-this-plan | `templates/common/.github/workflows/verify.yml` provides a reusable generated-app workflow. |
| Add empty, loading, and error primitives | open-fixed-by-this-plan | React template includes reusable state primitives with tests. |

## Other Findings

| Finding | Status | Closure |
| --- | --- | --- |
| No project-level MCP configured | accepted-decision | Project-level MCP remains absent until repo-specific tooling is needed; personal tools belong in user-level Codex config. |
| No automated secret scan | open-fixed-by-this-plan | `scripts/scan-secrets.ps1` provides local regex scanning with optional `gitleaks`, and CI runs it. |
| ZIP includes `.git/` | open-fixed-by-this-plan | `scripts/export-workspace.ps1` creates distributable archives excluding `.git/`, generated projects, dependencies, logs, reports, and wrapper-folder duplication. |
| `scripts/verify-app.ps1` lacks dependency detection, bun, JSON classification, and failure classes | open-fixed-by-this-plan | Verification script reports missing dependencies, missing scripts, command failures, no-checks-ran, and supports Bun lockfiles. |
| `scripts/test-workspace.ps1` only tests React template | open-fixed-by-this-plan | Workspace test scaffolds and inspects React, Next, and Expo templates. |

## Closeout Rule

This ledger is complete only when these commands pass from the workspace root:

```powershell
.\scripts\check-workspace.ps1
.\scripts\validate-codex-assets.ps1 -RequirePythonToml $true
.\scripts\test-hooks.ps1
.\scripts\scan-secrets.ps1
.\scripts\test-workspace.ps1
```

## React Vite Capacitor Template Finalization 2026-07-07

This branch intentionally bundles the React/Vite/Capacitor template finalization with the broader workspace-audit closeout that the same verification gate depends on. The generated-app CI template, planning assets, secret scan/export scripts, and Next/Expo template baselines were kept in one branch because `scripts/check-workspace.ps1`, `scripts/validate-codex-assets.ps1`, and `scripts/test-workspace.ps1` validate them together as one control-workspace surface.

- Dependencies finalized in `templates/react-vite-capacitor`: `@supabase/supabase-js`, `@t3-oss/env-core`, `@hookform/resolvers`, shadcn/ui source dependencies, Recharts. `@capacitor/android` and `@capacitor/ios` were removed from always-on template dependencies and moved to the per-app native setup flow.
- Commands verified from the template directory: `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, `npm run e2e`.
- Disposable generated-app verification passed through `scripts/test-workspace.ps1`, including `npm install` and `..\..\scripts\verify-app.ps1 -ProjectPath .` inside `projects/__verify-react-vite-capacitor`.
- Native platform policy: `android/` and `ios/` stay per generated app only. The shared template now excludes those folders from `create-app.ps1` copies and provides `templates/react-vite-capacitor/scripts/add-native-platforms.ps1` for opt-in setup.
- Task 2 closeout note: the independent re-review subagent was unavailable because of a usage-limit failure, so closeout was performed with fresh local verification of the shadcn source, theme-token fix, build/test checks, and 1440/1280/768/390 rendered browser evidence.

## Review Rebaseline 2026-07-08

- PR merge finding: closed. PR #1 merged to `main` on 2026-07-07, so this branch is being reviewed for follow-on merge rather than treated as unmerged shadow work from the original audit.
- `standards/workspace.md` structure finding: closed. The current workspace standard documents `docs/`, `PLANS.md`, and `templates/PLAN.template.md` in the root control-workspace shape.
- ESLint module-boundary finding: open-fixed-by-this-plan. The React/Vite/Capacitor template now enforces `@/modules/<module>` as the public cross-module API surface and rejects deep imports into another module's internals.
